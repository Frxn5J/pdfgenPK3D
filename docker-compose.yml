version: '3.8'

services:
  catalog:
    build: .
    restart: always
    ports:
      - "3001:3000"
    volumes:
      - catalog_data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      

volumes:
  catalog_data:
